apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-external-database-access
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes:
    - Ingress
  ingress:
    - from:
      - podSelector: {}
