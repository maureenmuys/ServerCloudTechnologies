docker image builden 
--------------------

docker image maken
------------------
docker build -t user-wishlist-k8s:v1 .


docker image naar taggen
------------------------
docker login
docker tag user-wishlist-k8s maureenmuys/user-wishlist-k8s

docker image naar dock hub pushen 
---------------------------------
docker push user-wishlist-k8s:v1 maureenmuys/user-wishlist-k8s:v1


2 create cluseter in google cloud:
Kubernetes Engine -> Create 
Name:user-wishlist-cluster
Region:europe-west1


3.install helm
gcloud container clusters get-credentials user-wishlist-cluster --zone=europe-west1 --project=servercloudtech
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm install my-release ingress-nginx/ingress-nginx
kubectl --namespace default get services -o wide -w my-release-ingress-nginx-controller


4. add password ???
kubectl create secret generic pgpassword --from-literal=PGPASSWORD=hello_flask

4. get application from git
git clone https://github.com/maureenmuys/ServerCloudTechnologies.git
cd ServerCloudTechnologies
cd 

5. install yml files
kubectl apply -f k8s/

6. open application 
 go to Ingress
click on link 


command to check status 
------------------------
kubectl get secrets
kubectl get pods
kubectl get services
kubectl get pvc
kubectl get ingress





docker tag user-wishlist-k8s maureenmuys/user-wishlist-k8s:v1


kubectl create secret tls your-secret-name --cert=path/to/your_certificate.crt --key=path/to/your_private_key.key
